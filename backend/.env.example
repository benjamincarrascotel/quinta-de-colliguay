APP_NAME="Quinta de Colliguay"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=America/Santiago
APP_URL=http://localhost
APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_CL

LOG_CHANNEL=daily
LOG_LEVEL=debug
LOG_DAILY_DAYS=14

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=quinta_colliguay
DB_USERNAME=postgres
DB_PASSWORD=secret

CACHE_STORE=database
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=120

# SMTP Configuration (to be configured later)
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="reservas@quintacolliguay.cl"
MAIL_FROM_NAME="${APP_NAME}"

# Reservation Parameters (can be modified via admin panel)
RESERVATION_ADULT_PRICE=20000
RESERVATION_CHILD_PRICE=10000
RESERVATION_MIN_ADULTS=20
RESERVATION_MAX_TOTAL=60
RESERVATION_MIN_NIGHTS=2
RESERVATION_BUFFER_HALF_DAY=true

# Admin Notification Email
ADMIN_EMAIL="admin@quintacolliguay.cl"

# WhatsApp Admin Number (without +56)
ADMIN_WHATSAPP=912345678
